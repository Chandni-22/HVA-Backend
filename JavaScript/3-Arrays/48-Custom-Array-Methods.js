// a) forEachArray Function:
function forEachArray(array, callback){
    for (let i=0; i<array.lenght; i++){
        callback(array[i]);
    }
};

const num=[1,2,3];
forEachArray(num,function())
// b) mapArray Function:

// c) filterArray Function: